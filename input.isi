programa

  numero a, b, c, d, e, f.
  texto  t1, t2.

  leia(a).
  leia(b).
  leia(t1).
  
  a := 1 + 2 * 3 / b.
  
  t1 := "".

  t2 := "oi".
  
  escreva("oi").

  se ( a > b ) entao {
     escreva (a).
  }
  senao {
     escreva (b).
  }
  
  enquanto (a < b) {
  	a := a + b.
  	a := a + b.
  	a := a + b.
  	a := a + b.
  	a := a + b.
  }

fimprog.