programa

  numero a, b, c, d, e, f.

  texto  t1, t2.

  leia(a).
  leia(b).
  leia(t1).
  
  /*comentário
  linha
  */
   
   
  a := 1 + 2 * 3 / b.
  
  d:= 3.14.
  

  t2 := "oi".
  
  escreva("oi").


  se ( a > b ) entao {
     escreva (a).
  }
  senao {
     escreva (b).
  }
  
  enquanto (a < b) {
  	a := a + b.
  	a := a + b.
  	a := a + b.
  	a := a + b.
  	a := a + b.
  }

fimprog.