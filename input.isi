programa

  numero a, b, c.
  texto  t1.

  leia(a).
  leia(b).
  
  a := 1 + 2 * 3 / b.


  se ( a > b ) {
     escreva (a).
  }
  senao {
     escreva (b).
  }
  
  enquanto (a < b) {
  	a := a + b.
  	a := a + b.
  	a := a + b.
  	a := a + b.
  	a := a + b.
  }

fimprog.